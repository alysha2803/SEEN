<?xml version="1.0" encoding="utf-8"?>
<!--
    SEEN — no sensitive permissions. This app accesses no real device data.
    Do NOT add: ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION, READ_CONTACTS,
    CAMERA, RECORD_AUDIO, READ_SMS, or any other sensitive permission.
    All content is self-contained static fiction.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SEEN">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
